{% extends 'login.twig' %}
{% block content %}
    <h1 class="brand-title">Passwort ändern</h1>
    <p>Hier kannst du dein Passwort ändern:</p>
    <form action="{{ path(reset_route, { 'token': token }) }}" class="pure-form pure-form-aligned" method="post">
        <div class="pure-control-group">
            <label for="password">Passwort</label>
            <input type="password" name="password" id="password" value=""/>
        </div>
        <div class="pure-control-group">
            <label for="password_repeat">Passwort Wiederholung</label>
            <input type="password" name="password_repeat" id="password_repeat" value=""/>
        </div>
        <div class="pure-controls">
            <button type="submit" class="pure-button pure-button-primary" value="Passwort ändern">
                <i class="icon-login font-icon">&#xe80e;</i>&ensp;Passwort ändern
            </button>
        </div>
    </form>
{% endblock %}